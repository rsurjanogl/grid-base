class GridCore {
  id: string;

  constructor(public config: any = {}) {

    // TODO implement UUID
    this.id = '12345';
  }
  // Lifecycle init
  // this.events.onInit();
  // Lifecycle created
  // Lifecycle beforeCompile
  // Lifecycle compiled
  // Lifecycle ready
  // Lifecycle attached
  // Lifecycle detached
  // Lifecycle beforeDestroy
  // Lifecycle destroyed
  init() {

  }
}





export {GridCore};
